// import { isObject } from '@/utils/check'
export const hasPermission = {
  inserted (el, binding, vnode) {
    // const types = vnode.context.$route.meta.resourceList
    // const values = binding.value
    // if (isObject(values)) {
    //   if (!types.includes(values.role)) {
    //     el.setAttribute('disabled', 'disabled')
    //     el.className = el.className + ' is-disabled'
    //   }
    // } else {
    //   if (!types.includes(values)) {
    //     el.parentNode.removeChild(el)
    //   }
    // }
  }
}
